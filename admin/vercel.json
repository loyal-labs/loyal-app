{
  "ignoreCommand": "if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] || ! git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA\" 2>/dev/null; then exit 1; fi; git diff \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD --quiet -- . ../packages ../app/src/features/private-transfer-analytics",
  "installCommand": "rm -rf node_modules && cd .. && bun install --frozen-lockfile",
  "buildCommand": "bun run build"
}
